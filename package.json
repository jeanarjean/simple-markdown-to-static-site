{
  "name": "simple-markdown-to-static-site-generator",
  "version": "1.0.0",
  "description": "Turn a folder and its markdown files into an easy to read static website.",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "preferGlobal": true,
  "bin": {
    "gen-static-site": "./dist/index.js"
  },
  "dependencies": {
    "@types/node": "^12.0.12",
    "chalk": "^2.4.2"
  },
  "devDependencies": {
    "ts-node": "^8.3.0",
    "typescript": "^3.5.2"
  }
}
